package com.example.copicatkurilshika.ViberSender;

public class ViberSender {


    /*private Logger logger = LoggerFactory.getLogger(getClass());

    public void sendDr(@NotNull String token, @NotNull String drUrl, @NotNull ViberDr dr) {
        try {
            sendDr(token, drUrl, converter.asJson(dr));
        } catch (Exception e) {
            logger.error("token={}, {}", token, e.getMessage(), e);
        }

        public void sendSeen(@NotNull String token, @NotNull String drUrl, @NotNull ViberDr dr) {
            try {
                sendDr(token, drUrl, converter.asJson(dr));
            } catch (Exception e) {
                logger.error("token={}, {}", token, e.getMessage(), e);
            }*/

}
